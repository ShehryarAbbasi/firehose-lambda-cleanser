{
  "name": "firehose-dso-cleanser",
  "description": "cleanses data sent from Kinesis Firehose",
  "version": "0.0.2",
  "private": true,
  "scripts": {
    "test": "nyc --reporter=html --reporter=text --check-coverage --lines 80 --functions 90 --branches 50 mocha --reporter=mochawesome --timeout 5000"
  },
  "dependencies": {},
  "devDependencies": {
    "mocha": "^5.2.0",
    "mochawesome": "^3.1.1",
    "nyc": "^13.1.0"
  }
}
